{
    "name": "ss-mood-player-tooling",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "Development tooling only. The application in app/src has no dependencies and no build step.",
    "scripts": {
        "serve": "java .claude/skills/web-conventions/scripts/zws app/src",
        "test": "node --test",
        "test:e2e": "playwright test",
        "typecheck": "tsc -p app/src/jsconfig.json",
        "vendor:lit-html": "./tools/update-lit-html.sh",
        "vendor:spessasynth": "./tools/update-spessasynth.sh"
    },
    "devDependencies": {
        "@playwright/test": "1.56.1",
        "typescript": "^6.0.2"
    }
}
